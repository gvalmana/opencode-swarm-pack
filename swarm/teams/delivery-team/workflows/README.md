# Workflows

Portable workflow definitions for `delivery-team` will live here.

Planned workflow:

- `delivery`: `coder -> cleaner -> final`

Workflow files should describe orchestration steps without target-specific slash command syntax.
